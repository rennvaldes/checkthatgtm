services:
  - type: web
    name: site-frontend
    runtime: node
    plan: starter
    buildCommand: |
      NODE_ENV=production GENERATE_SOURCEMAP=true npm install --include=dev
      NODE_ENV=production GENERATE_SOURCEMAP=true npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
